{
	"builds": [
		{
			"src": "src/main.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"dest": "src/main.ts",
			"methods": ["GET", "POST", "PUT", "PATCH", "HEAD", "DELETE", "OPTIONS"],
			"src": "/(.*)"
		}
	],
	"version": 2
}
